# 260721


- 繼續實施任務 3.1-3.7 使用者驗證


# 4.1~7.6

實作完成情況

已完成的內容（任務 4.1~7.6）：

✅ 4.1-4.3 清單與資料夾管理 Server Actions
- src/actions/lists.ts：
  - createList(name, color, folderId?) - 建立清單
  - updateList(id, data) - 編輯清單
  - deleteList(id) - 刪除清單（任務自動移至收集箱）
  - getLists() - 查詢清單（含未完成任務數）
  - createFolder(name) - 建立資料夾
  - updateFolder(id, name) - 編輯資料夾
  - deleteFolder(id) - 刪除資料夾
  - getFolders() - 查詢資料夾與內含清單

✅ 4.4 清單側欄 UI
- src/components/tasks/Sidebar.tsx：智慧清單導覽、清單列表（含顏色圓點與計數）、資料夾摺疊/展開、標籤篩選、新增清單/資料夾/標籤按鈕

✅ 5.1-5.2 任務管理 Server Actions
- src/actions/tasks.ts：
  - createTask(title, listId?, ...) - 建立任務
  - updateTask(id, data) - 編輯任務
  - deleteTask(id) - 刪除任務
  - toggleTaskComplete(id, completed) - 標記完成/取消完成（含 completedAt）
- src/components/tasks/TaskRow.tsx：任務列表列（勾選框、標題、到期日、優先級旗標）
- src/components/tasks/TaskListView.tsx：任務列表檢視（支援已過期分組）
- src/components/tasks/TaskDetailPanel.tsx：詳情面板（編輯所有欄位、刪除、標籤指派）

✅ 6.1-6.3 標籤管理 Server Actions + UI
- src/actions/tags.ts：
  - createTag(name, color) - 建立標籤
  - updateTag(id, data) - 編輯標籤
  - deleteTag(id) - 刪除標籤
  - getTags() - 查詢標籤
  - assignTagToTask(taskId, tagId) - 指派標籤
  - removeTagFromTask(taskId, tagId) - 移除標籤
- Sidebar 中的標籤篩選

✅ 7.1-7.6 智慧清單 + 順延
- src/actions/tasks.ts：
  - getTasksForView(view) - 支援 'today'、'next7days'、'inbox'、'completed'、list、tag 等視圖
  - getTodayViewGrouped() - 今天視圖（包含過期分組）
  - postponeTask(id) - 順延動作
- Sidebar 中的智慧清單導覽
- TaskListView 中的已過期分組與順延按鈕

✅ UI 容器 & 頁面
- src/components/tasks/TasksApp.tsx：主容器，管理視圖狀態、任務刷新、表單模態
- src/app/(app)/tasks/page.tsx：Server component，取得初始資料並渲染 TasksApp
- src/lib/date.ts：日期工具函數（今天範圍、過期判定、格式化）

✅ 所有 Server Actions 的登入保護
- 每個 Server Action 都呼叫 requireAuth() 進行權限檢查

✅ 型別安全
- TypeScript strict 模式通過，無編譯錯誤
- ESLint 無警告

✅ 建構成功
- npm run build 成功，所有頁面已編譯

已標記完成的任務（tasks.md 4.1~7.6）都已打勾，8/9/10 未修改（符合用戶要求停在 7.6）。
